

var handlers = {
	error:function(){},
	exit:function(){},
	close:function(){},
	disconnect:function(){},
	message:function(){}	
}

var bindHandlers = function(child){
	
	//child.on('error', 
	//child.on('exit', 
	//child.on('close', 
	//child.on('disconnect', 
	//child.on('message', 
}

module.exports = function(child){
	
}